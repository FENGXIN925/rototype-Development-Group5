## file path:
## ~/catkin_ws/src/fall_monitor/scripts
## 1. roscore e
## 2. roslaunch astra_camera astra.launch  # open robot camera
## 3. roslaunch jupiterobot2_vision_mediapipe mediapipe_pose.launch image:=/camera/color/image_raw  # structure detection point
## 4. rosrun sound_play soundplay_node.py  # sound play
## 5. roslaunch fall_monitor fall_detection_with_telegram_en.launch  # fall_down detection
## 5. roslaunch fall_monitor fall_detection_with_telegram_en2.launch
## 

## ~/catkin_ws/src/jupiterobot2/jupiterobot2_vision/jupiterobot2_vision_mediapipe # structure detection point


# 1.进入工作区域
cd ~/ros2_ws
# 2.编译包
colcon build --packages-select fall_monitor_agents
# 3.Source
source /home/mustar/catkin_ws/devel/setup.bash

# 4.启动所有Agent
ros2 launch fall_monitor_agents ai_agent_launch.py
# 5.单节点启动
# 只跑感知模块
ros2 run fall_monitor_agents perception_agent  
# 只跑决策模块
ros2 run fall_monitor_agents decision_agent --ros-args -p yolo_model_path:=/path/to/your/model.pt  
# 只跑执行模块
ros2 run fall_monitor_agents execution_agent  


cd ~/catkin_ws
catkin_make  # 或 catkin build
source devel/setup.bash
ros2 launch jupiterobot2_vision ai_agent_launch.py

# 完整的命令：
# 1. 进入工作区
cd ~/catkin_ws

# 2. 构建只针对你的包（加速编译）
colcon build --packages-select jupiterobot2_vision

# 3. Source 环境（每开一个新终端都得执行）
source install/setup.bash

# 4. 一键启动三大 Agent 节点
ros2 launch jupiterobot2_vision ai_agent_launch.py \
    yolo_model_path:=~/catkin_ws/src/jupiterobot2/jupiterobot2_vision/models/yolov5_fall.pt



